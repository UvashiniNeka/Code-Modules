const eventName = "Community Meetup";
const eventDate = "2025-06-15";
let seats = 50;

let info = `${eventName} on ${eventDate}, Seats left: ${seats}`;
console.log(info);

seats--;
console.log(`Seats left after registration: ${seats}`);
